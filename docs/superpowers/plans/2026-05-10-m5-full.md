# M5 完整版實作計劃

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 M5 minimal 切段（partial 比例 ~15%）升級成完整版 — partial chunk 自動遞迴切半 retry + 並行 chunk 推論，達到 partial fail < 3%、處理時間 10-20s（10 分鐘檔）。

**Architecture:** 改 `audio_splitter.py` 加切半 helper、改 `job_runner.py` 把序列 for loop 改 `asyncio.gather` + `asyncio.Semaphore` + 遞迴 `transcribe_with_retry`。Job 表既有 `chunks_total` / `chunks_done` 語義不變（只看原始切數、retry sub-chunks 內部不外暴露）。

**Tech Stack:** Python 3.11、SQLAlchemy 2.0 async、asyncio、httpx、ffmpeg subprocess、pytest + pytest-asyncio。

---

## File Structure

| 動作 | 檔案 | 責任 |
|---|---|---|
| Modify | `backend/app/config.py` | 加 `chunk_concurrency` / `chunk_retry_max_depth` settings |
| Modify | `.env.example` | 對應 env 範例 |
| Modify | `backend/app/services/audio_splitter.py` | 新增 `split_chunk_in_half` helper |
| Modify | `backend/app/services/job_runner.py` | 改 `_transcribe_all_chunks` 用並行 + retry；新增 `ChunkOutcome` dataclass 跟 `transcribe_with_retry` 遞迴函數 |
| Create | `backend/tests/test_chunk_retry.py` | retry 邏輯 unit test |
| Modify | `backend/tests/test_audio_splitter.py` | 加 `split_chunk_in_half` test |

每個 commit 用拆 3 條獨立 git 命令（add / commit / push），避開 Claude Code sandbox 對 chain 的攔截。

---

## Task 1: 加 settings — chunk_concurrency / chunk_retry_max_depth

**Files:**
- Modify: `backend/app/config.py`
- Modify: `.env.example`
- Test: `backend/tests/test_config.py`

- [ ] **Step 1: 寫 failing test**

加進 `backend/tests/test_config.py`（如無則新建）：

```python
def test_chunk_concurrency_default_is_8():
    from app.config import get_settings
    get_settings.cache_clear()
    s = get_settings()
    assert s.chunk_concurrency == 8


def test_chunk_retry_max_depth_default_is_2():
    from app.config import get_settings
    get_settings.cache_clear()
    s = get_settings()
    assert s.chunk_retry_max_depth == 2
```

- [ ] **Step 2: Run test verify fail**

```bash
docker compose exec backend pytest tests/test_config.py::test_chunk_concurrency_default_is_8 -v
```

Expected: FAIL with `AttributeError: 'Settings' object has no attribute 'chunk_concurrency'`

- [ ] **Step 3: 實作 — settings field**

`backend/app/config.py` Audio section 加：

```python
    # === ASR Pipeline ===
    # 並行 chunk 推論上限（含 retry sub-chunks 共享同一 semaphore）。
    # 預設 8：vLLM max_num_seqs=64 的 1/8、留空間給 admin 同時 transcribe。
    chunk_concurrency: int = 8
    # partial chunk 偵測後遞迴切半 retry 的最大深度（0=不 retry，2=最多 4 個 sub-sub-chunks）
    chunk_retry_max_depth: int = 2
```

`.env.example` 加 section：

```bash
# === ASR Pipeline ===
CHUNK_CONCURRENCY=8
CHUNK_RETRY_MAX_DEPTH=2
```

- [ ] **Step 4: Run test verify pass**

```bash
docker compose exec backend pytest tests/test_config.py -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C "D:/vibevoice_asr" add backend/app/config.py .env.example backend/tests/test_config.py
git -C "D:/vibevoice_asr" commit -m "feat(config): chunk_concurrency / chunk_retry_max_depth settings"
git -C "D:/vibevoice_asr" push origin main
```

---

## Task 2: split_chunk_in_half helper

**Files:**
- Modify: `backend/app/services/audio_splitter.py`
- Test: `backend/tests/test_audio_splitter.py`

- [ ] **Step 1: 寫 failing test**

加進 `backend/tests/test_audio_splitter.py`：

```python
def test_split_chunk_in_half_offsets_correct():
    """parent 55s chunk 切半 → 兩個 sub-chunks，offset 在 parent 全域時間軸內。"""
    from app.services.audio_splitter import Chunk, split_chunk_in_half_metadata

    parent = Chunk(
        path=Path("/tmp/parent.mp3"),
        start_offset_sec=100.0,
        end_offset_sec=155.0,
        is_split=True,
    )
    subs = split_chunk_in_half_metadata(parent, overlap_sec=5.0)
    assert len(subs) == 2

    # 第一個 sub: 100 → 130（30s 含 5s overlap 進入第二段）
    assert subs[0].start_offset_sec == 100.0
    assert subs[0].end_offset_sec == 130.0
    # 第二個 sub: 125 → 155（5s overlap）
    assert subs[1].start_offset_sec == 125.0
    assert subs[1].end_offset_sec == 155.0
    # 兩段覆蓋整個 parent 範圍
    assert subs[0].end_offset_sec >= subs[1].start_offset_sec  # 有 overlap
    assert subs[1].end_offset_sec == parent.end_offset_sec


def test_split_chunk_in_half_min_duration_clamps_overlap():
    """parent 太短（譬如 8s）時 overlap 不能超過 chunk_dur，否則 sub 無意義。"""
    from app.services.audio_splitter import Chunk, split_chunk_in_half_metadata

    parent = Chunk(
        path=Path("/tmp/short.mp3"),
        start_offset_sec=0.0,
        end_offset_sec=8.0,
        is_split=True,
    )
    subs = split_chunk_in_half_metadata(parent, overlap_sec=5.0)
    # 8s parent、5s overlap 不合理 → 自動降 overlap 到 chunk_dur 的 1/3 以下
    # sub 1: 0-5、sub 2: 3-8（overlap 2s）
    assert len(subs) == 2
    assert subs[0].end_offset_sec - subs[1].start_offset_sec >= 1.0  # overlap >= 1s
    assert subs[0].end_offset_sec - subs[0].start_offset_sec >= 4.0  # sub 不會太短
```

- [ ] **Step 2: Run test verify fail**

```bash
docker compose exec backend pytest tests/test_audio_splitter.py::test_split_chunk_in_half_offsets_correct -v
```

Expected: FAIL with `ImportError: cannot import name 'split_chunk_in_half_metadata'`

- [ ] **Step 3: 實作**

`backend/app/services/audio_splitter.py` 末端加：

```python
def split_chunk_in_half_metadata(
    parent: Chunk,
    overlap_sec: float = 5.0,
) -> list[Chunk]:
    """把 parent chunk metadata 切半（不真的執行 ffmpeg）— 回兩個 sub-chunk Chunk
    instances 帶正確 start/end offset。

    為何單獨抽 metadata helper：caller（transcribe_with_retry）需要先拿 sub
    metadata 才能跑 ffmpeg 切實體 sub file。先決定切點再做 IO 比較好測。

    Sub 路徑暫設 parent.path（caller 後續會 ffmpeg 寫到實際 sub_dir）。
    """
    parent_dur = parent.end_offset_sec - parent.start_offset_sec
    if parent_dur <= 0:
        raise ValueError(f"parent chunk has non-positive duration: {parent_dur}")

    # overlap 不可超過 chunk_dur 的 1/3（避免 sub 太短）
    sub_dur = parent_dur / 2 + overlap_sec / 2
    safe_overlap = min(overlap_sec, parent_dur / 3)
    sub_dur = parent_dur / 2 + safe_overlap / 2

    mid = parent.start_offset_sec + parent_dur / 2
    sub_a_start = parent.start_offset_sec
    sub_a_end = mid + safe_overlap / 2
    sub_b_start = mid - safe_overlap / 2
    sub_b_end = parent.end_offset_sec

    return [
        Chunk(
            path=parent.path,  # caller 重設
            start_offset_sec=sub_a_start,
            end_offset_sec=sub_a_end,
            is_split=True,
        ),
        Chunk(
            path=parent.path,
            start_offset_sec=sub_b_start,
            end_offset_sec=sub_b_end,
            is_split=True,
        ),
    ]


def split_chunk_in_half(
    parent: Chunk,
    sub_dir: Path,
    depth: int,
    overlap_sec: float = 5.0,
) -> list[Chunk]:
    """把 parent chunk 切半成兩個實體 sub MP3 file，回 Chunk list 含寫好的 path。

    sub_dir 是 caller 指定的子目錄（譬如 chunks_dir / f"depth_{depth}"），
    file 命名 `chunk_{parent.start_offset_sec:.0f}_sub_{i}.mp3`。
    """
    sub_metas = split_chunk_in_half_metadata(parent, overlap_sec=overlap_sec)
    sub_dir.mkdir(parents=True, exist_ok=True)
    out: list[Chunk] = []
    for i, meta in enumerate(sub_metas):
        sub_path = sub_dir / f"chunk_{int(parent.start_offset_sec)}_sub_{i}.mp3"
        # 從 parent file 切（parent.path 是已存在的 mp3）
        relative_start = meta.start_offset_sec - parent.start_offset_sec
        relative_dur = meta.end_offset_sec - meta.start_offset_sec
        _ffmpeg_extract_chunk(
            parent.path, sub_path, relative_start, relative_dur,
            chunk_index=i, cleanup_dir=sub_dir,
        )
        out.append(Chunk(
            path=sub_path,
            start_offset_sec=meta.start_offset_sec,
            end_offset_sec=meta.end_offset_sec,
            is_split=True,
        ))
    return out
```

- [ ] **Step 4: Run test verify pass**

```bash
docker compose exec backend pytest tests/test_audio_splitter.py -v
```

Expected: PASS（new tests + 既有 splitter tests 全通過）

- [ ] **Step 5: Commit**

```bash
git -C "D:/vibevoice_asr" add backend/app/services/audio_splitter.py backend/tests/test_audio_splitter.py
git -C "D:/vibevoice_asr" commit -m "feat(splitter): split_chunk_in_half helper for retry recursion"
git -C "D:/vibevoice_asr" push origin main
```

---

## Task 3: ChunkOutcome dataclass

**Files:**
- Modify: `backend/app/services/job_runner.py`
- Test: `backend/tests/test_chunk_retry.py`（新檔）

- [ ] **Step 1: 寫 failing test**

`backend/tests/test_chunk_retry.py`：

```python
"""Tests for chunk-level retry logic in job_runner."""
from __future__ import annotations

from pathlib import Path

import pytest


def test_chunk_outcome_dataclass_shape():
    from app.services.job_runner import ChunkOutcome

    o = ChunkOutcome(
        segments=[{"start_time": 0.0, "end_time": 1.0, "speaker_id": 1, "text": "x"}],
        raw_text='[{"Start":0,"End":1,"Speaker":0,"Content":"x"}]',
        partial=False,
        depth_reached=0,
        attempts=1,
    )
    assert o.partial is False
    assert o.depth_reached == 0
    assert o.attempts == 1
    assert len(o.segments) == 1
```

- [ ] **Step 2: Run test verify fail**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py -v
```

Expected: FAIL with `ImportError: cannot import name 'ChunkOutcome'`

- [ ] **Step 3: 實作**

`backend/app/services/job_runner.py` 頂部 import 區後加：

```python
from dataclasses import dataclass, field


@dataclass
class ChunkOutcome:
    """單一 chunk（含 retry 後）的轉錄結果。

    fields:
      segments: 該 chunk（已加 offset）的 segment list
      raw_text: vLLM 原始輸出（多次 retry 時用 separator 串接）
      partial: 達 retry 上限仍 partial 為 True
      depth_reached: 實際遞迴到的最深深度（0 = 不 retry、N = retry N 次）
      attempts: 該 chunk 累計 vLLM 呼叫次數（含所有 retry sub-chunks）
    """

    segments: list[dict] = field(default_factory=list)
    raw_text: str = ""
    partial: bool = False
    depth_reached: int = 0
    attempts: int = 0
```

- [ ] **Step 4: Run test verify pass**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py -v
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git -C "D:/vibevoice_asr" add backend/app/services/job_runner.py backend/tests/test_chunk_retry.py
git -C "D:/vibevoice_asr" commit -m "feat(job-runner): ChunkOutcome dataclass for retry results"
git -C "D:/vibevoice_asr" push origin main
```

---

## Task 4: transcribe_with_retry 遞迴函數

**Files:**
- Modify: `backend/app/services/job_runner.py`
- Test: `backend/tests/test_chunk_retry.py`

- [ ] **Step 1: 寫 failing test（mock vllm_client）**

`backend/tests/test_chunk_retry.py` 加：

```python
import asyncio
from unittest.mock import AsyncMock, MagicMock, patch


@pytest.fixture
def chunk_factory(tmp_path):
    """建一個 fake chunk + 對應 file（內容是空 mp3 header、ffmpeg 不會真的跑）。"""
    def _make(start: float, end: float, content: bytes = b""):
        from app.services.audio_splitter import Chunk
        f = tmp_path / f"chunk_{int(start)}.mp3"
        f.write_bytes(content)
        return Chunk(
            path=f, start_offset_sec=start, end_offset_sec=end, is_split=True,
        )
    return _make


@pytest.mark.asyncio
async def test_no_retry_when_partial_false(chunk_factory):
    """vLLM 回 partial=False → 不 retry、depth_reached=0。"""
    from app.services.job_runner import transcribe_with_retry

    chunk = chunk_factory(0, 55)
    sem = asyncio.Semaphore(1)
    fake_vllm = AsyncMock(return_value={
        "raw_text": '[{"Start":0,"End":1,"Speaker":0,"Content":"hello"}]',
        "elapsed_sec": 0.1, "attempts": 1, "partial": False,
    })

    with patch("app.services.job_runner.VllmClient") as MockClient:
        MockClient.return_value.transcribe = fake_vllm
        outcome = await transcribe_with_retry(
            chunk, depth=0, max_depth=2, sem=sem,
            vllm_base_url="http://mock", hotwords=[],
        )

    assert outcome.partial is False
    assert outcome.depth_reached == 0
    assert outcome.attempts == 1
    assert len(outcome.segments) == 1


@pytest.mark.asyncio
async def test_retry_once_then_success(chunk_factory):
    """depth 0 partial → depth 1 兩個 sub 都 success → outcome.partial=False。"""
    from app.services.job_runner import transcribe_with_retry

    chunk = chunk_factory(0, 55)
    sem = asyncio.Semaphore(2)

    call_count = {"n": 0}
    async def vllm_mock(audio_bytes, mime, dur, hotwords):
        call_count["n"] += 1
        if call_count["n"] == 1:
            # depth 0: partial
            return {"raw_text": '[]', "elapsed_sec": 0.1, "attempts": 3, "partial": True}
        # depth 1 sub_a / sub_b: success
        return {
            "raw_text": '[{"Start":0,"End":1,"Speaker":0,"Content":"sub"}]',
            "elapsed_sec": 0.1, "attempts": 1, "partial": False,
        }

    # 同時 mock split_chunk_in_half 不真的呼 ffmpeg
    fake_subs = [chunk_factory(0, 30), chunk_factory(25, 55)]
    with patch("app.services.job_runner.VllmClient") as MockClient, \
         patch("app.services.job_runner.split_chunk_in_half", return_value=fake_subs):
        MockClient.return_value.transcribe = AsyncMock(side_effect=vllm_mock)
        outcome = await transcribe_with_retry(
            chunk, depth=0, max_depth=2, sem=sem,
            vllm_base_url="http://mock", hotwords=[],
        )

    assert outcome.partial is False
    assert outcome.depth_reached == 1
    assert outcome.attempts == 3  # depth 0 fail (1) + depth 1 sub_a (1) + sub_b (1)


@pytest.mark.asyncio
async def test_retry_twice_reaches_max_depth(chunk_factory):
    """所有層都 partial → 達 max_depth、partial=True、不再 retry。"""
    from app.services.job_runner import transcribe_with_retry

    chunk = chunk_factory(0, 55)
    sem = asyncio.Semaphore(4)
    always_partial = AsyncMock(return_value={
        "raw_text": '[]', "elapsed_sec": 0.1, "attempts": 3, "partial": True,
    })
    fake_subs_d1 = [chunk_factory(0, 30), chunk_factory(25, 55)]
    fake_subs_d2 = [chunk_factory(0, 17), chunk_factory(13, 30)]

    def split_side_effect(parent, sub_dir, depth, overlap_sec=5.0):
        return fake_subs_d1 if depth == 1 else fake_subs_d2

    with patch("app.services.job_runner.VllmClient") as MockClient, \
         patch("app.services.job_runner.split_chunk_in_half", side_effect=split_side_effect):
        MockClient.return_value.transcribe = always_partial
        outcome = await transcribe_with_retry(
            chunk, depth=0, max_depth=2, sem=sem,
            vllm_base_url="http://mock", hotwords=[],
        )

    assert outcome.partial is True
    assert outcome.depth_reached == 2  # 達上限 2
```

- [ ] **Step 2: Run test verify fail**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py -v
```

Expected: 3 個新 test FAIL with `ImportError: cannot import name 'transcribe_with_retry'`

- [ ] **Step 3: 實作 — transcribe_with_retry**

`backend/app/services/job_runner.py` 加：

```python
from app.services.audio_splitter import Chunk, split_chunk_in_half


async def transcribe_with_retry(
    chunk: Chunk,
    *,
    depth: int,
    max_depth: int,
    sem: asyncio.Semaphore,
    vllm_base_url: str | list[str],
    hotwords: list[str],
) -> ChunkOutcome:
    """遞迴 chunk-level retry。

    depth 0 = 原始 chunk；達 max_depth 仍 partial 視為達上限、接受結果。
    sub-chunks 共用同一 semaphore 控全域併發。
    """
    audio_bytes, mime = await _load_chunk_audio(chunk)
    chunk_dur = chunk.end_offset_sec - chunk.start_offset_sec

    client = VllmClient(vllm_base_url)
    async with sem:
        result = await client.transcribe(audio_bytes, mime, chunk_dur, hotwords)

    segs, _ = parse_transcription(result["raw_text"])
    # offset 在這層加（segments 的 start_time 是 chunk 內部時間）
    for s in segs:
        s["start_time"] += chunk.start_offset_sec
        s["end_time"] += chunk.start_offset_sec

    if not result.get("partial") or depth >= max_depth:
        return ChunkOutcome(
            segments=segs,
            raw_text=result["raw_text"],
            partial=result.get("partial", False),
            depth_reached=depth,
            attempts=result["attempts"],
        )

    # Partial、未達上限 → 切半遞迴
    sub_dir = chunk.path.parent / f"depth_{depth + 1}"
    sub_chunks = split_chunk_in_half(chunk, sub_dir, depth + 1)
    sub_outcomes = await asyncio.gather(*[
        transcribe_with_retry(
            sc, depth=depth + 1, max_depth=max_depth, sem=sem,
            vllm_base_url=vllm_base_url, hotwords=hotwords,
        )
        for sc in sub_chunks
    ])

    # Merge sub outcomes（segments 已加完 offset、直接拼）
    merged_segs: list[dict] = []
    for o in sub_outcomes:
        merged_segs.extend(o.segments)
    merged_segs.sort(key=lambda s: s["start_time"])

    return ChunkOutcome(
        segments=merged_segs,
        raw_text="\n--- depth {} sub ---\n".format(depth + 1).join(
            o.raw_text for o in sub_outcomes
        ),
        partial=any(o.partial for o in sub_outcomes),
        depth_reached=max(o.depth_reached for o in sub_outcomes),
        attempts=result["attempts"] + sum(o.attempts for o in sub_outcomes),
    )
```

- [ ] **Step 4: Run test verify pass**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py -v
```

Expected: 3 個新 test PASS

- [ ] **Step 5: Commit**

```bash
git -C "D:/vibevoice_asr" add backend/app/services/job_runner.py backend/tests/test_chunk_retry.py
git -C "D:/vibevoice_asr" commit -m "feat(job-runner): transcribe_with_retry 遞迴 chunk-level retry"
git -C "D:/vibevoice_asr" push origin main
```

---

## Task 5: _transcribe_all_chunks 改用並行

**Files:**
- Modify: `backend/app/services/job_runner.py`
- Test: `backend/tests/test_chunk_retry.py`

- [ ] **Step 1: 寫 failing test**

`backend/tests/test_chunk_retry.py` 加：

```python
@pytest.mark.asyncio
async def test_concurrent_transcribe_via_semaphore(chunk_factory):
    """N 個 chunks 並行跑、Semaphore 控併發 ≤ chunk_concurrency。"""
    from app.services.job_runner import _transcribe_all_chunks
    from app.config import get_settings

    chunks = [chunk_factory(i * 50, (i + 1) * 50) for i in range(5)]
    state = {"job_id": "test", "hotwords": []}

    in_flight = {"max": 0, "current": 0}
    lock = asyncio.Lock()

    async def slow_transcribe(audio_bytes, mime, dur, hotwords):
        async with lock:
            in_flight["current"] += 1
            in_flight["max"] = max(in_flight["max"], in_flight["current"])
        await asyncio.sleep(0.05)  # 模擬 vLLM 處理
        async with lock:
            in_flight["current"] -= 1
        return {
            "raw_text": '[{"Start":0,"End":1,"Speaker":0,"Content":"x"}]',
            "elapsed_sec": 0.05, "attempts": 1, "partial": False,
        }

    settings = get_settings()
    settings.chunk_concurrency = 2  # 強制限 2 並發

    with patch("app.services.job_runner.VllmClient") as MockClient, \
         patch("app.services.job_runner._update_progress", new=AsyncMock()):
        MockClient.return_value.transcribe = AsyncMock(side_effect=slow_transcribe)
        outcome = await _transcribe_all_chunks(settings, state, 250.0, chunks)

    assert in_flight["max"] <= 2, f"Semaphore 沒擋住、max in flight = {in_flight['max']}"
    assert len(outcome["segments"]) == 5  # 每 chunk 一 segment、共 5
```

- [ ] **Step 2: Run test verify fail**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py::test_concurrent_transcribe_via_semaphore -v
```

Expected: FAIL（既有 `_transcribe_all_chunks` 是序列、無 Semaphore）

- [ ] **Step 3: 實作 — _transcribe_all_chunks 改並行**

替換 `backend/app/services/job_runner.py` 內的 `_transcribe_all_chunks`：

```python
async def _transcribe_all_chunks(
    settings: Settings,
    state: dict[str, Any],
    duration: float,
    chunks: list[Chunk],
) -> dict[str, Any]:
    """並行跑各 chunk（含 retry 遞迴），merge 後回 outcome。"""
    is_multi = len(chunks) > 1
    job_id = state["job_id"]

    if is_multi:
        await _update_progress(job_id, chunks_total=len(chunks), chunks_done=0)

    sem = asyncio.Semaphore(settings.chunk_concurrency)
    done_count = {"n": 0}
    done_lock = asyncio.Lock()

    async def run_one(idx: int, chunk: Chunk) -> ChunkOutcome:
        outcome = await transcribe_with_retry(
            chunk,
            depth=0,
            max_depth=settings.chunk_retry_max_depth,
            sem=sem,
            vllm_base_url=settings.vllm_base_url,
            hotwords=state["hotwords"],
        )
        if is_multi:
            async with done_lock:
                done_count["n"] += 1
                await _update_progress(
                    job_id, chunks_total=len(chunks), chunks_done=done_count["n"]
                )
        return outcome

    outcomes = await asyncio.gather(*[run_one(i, c) for i, c in enumerate(chunks)])

    # Merge：所有 outcomes 的 segments 已加 offset、合併排序
    merged_segs: list[dict] = []
    for o in outcomes:
        merged_segs.extend(o.segments)
    merged_segs.sort(key=lambda s: s["start_time"])

    return {
        "raw_text": _join_chunk_raw_texts([o.raw_text for o in outcomes]),
        "segments": merged_segs,
        "parser_debug": {"validation_warnings": []},
        "duration": duration,
        "attempts": sum(o.attempts for o in outcomes),
        "partial": any(o.partial for o in outcomes),
        "chunks_total": len(chunks),
    }
```

注意：原本 `_transcribe_all_chunks` 內有 `parse_transcription` 取 segments、現在
`transcribe_with_retry` 已經做了這層、ChunkOutcome.segments 帶 offset 處理過、不必
再 parse。

- [ ] **Step 4: Run test verify pass**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py -v
```

Expected: PASS

也跑全部 test 確認沒 regression：

```bash
docker compose exec backend pytest tests/ -v 2>&1 | tail -10
```

Expected: 所有既有 test + 新加 test 全 PASS

- [ ] **Step 5: Commit**

```bash
git -C "D:/vibevoice_asr" add backend/app/services/job_runner.py backend/tests/test_chunk_retry.py
git -C "D:/vibevoice_asr" commit -m "feat(job-runner): _transcribe_all_chunks 改用 asyncio.gather + Semaphore 並行"
git -C "D:/vibevoice_asr" push origin main
```

---

## Task 6: chunks_done 維持原始 chunks 語義驗證

**Files:**
- Test: `backend/tests/test_chunk_retry.py`

- [ ] **Step 1: 寫 test 確認 chunks_done 不含 retry sub-chunks**

```python
@pytest.mark.asyncio
async def test_chunks_done_counter_excludes_retry_sub_chunks(chunk_factory, tmp_path):
    """chunks_total / chunks_done 看原始 chunks、retry 內部不外暴露。"""
    from app.services.job_runner import _transcribe_all_chunks
    from app.config import get_settings

    chunks = [chunk_factory(0, 55), chunk_factory(50, 105)]
    state = {"job_id": "test_chunks_done", "hotwords": []}

    progress_calls: list[tuple[int, int]] = []

    async def fake_update(job_id, chunks_total, chunks_done):
        progress_calls.append((chunks_total, chunks_done))

    # 第一個 chunk depth 0 partial、retry 一次後 success
    # 第二個 chunk depth 0 直接 success
    call_count = {"n": 0}
    async def vllm_mock(audio_bytes, mime, dur, hotwords):
        call_count["n"] += 1
        # call 1 (chunk 0 depth 0): partial
        # call 2-3 (chunk 0 depth 1 sub_a / sub_b): success
        # call 4 (chunk 1 depth 0): success
        partial = (call_count["n"] == 1)
        return {
            "raw_text": '[{"Start":0,"End":1,"Speaker":0,"Content":"x"}]',
            "elapsed_sec": 0.05, "attempts": 1, "partial": partial,
        }

    fake_subs = [chunk_factory(0, 30), chunk_factory(25, 55)]
    settings = get_settings()
    settings.chunk_concurrency = 4
    settings.chunk_retry_max_depth = 1  # 限 1 次防進深層

    with patch("app.services.job_runner.VllmClient") as MockClient, \
         patch("app.services.job_runner.split_chunk_in_half", return_value=fake_subs), \
         patch("app.services.job_runner._update_progress", new=fake_update):
        MockClient.return_value.transcribe = AsyncMock(side_effect=vllm_mock)
        outcome = await _transcribe_all_chunks(settings, state, 105.0, chunks)

    # chunks_total 永遠 = 2（原始 chunks 數）、不會因為 retry 變成 4
    for total, done in progress_calls:
        assert total == 2

    # chunks_done 的最終值 = 2
    assert progress_calls[-1] == (2, 2)
    assert outcome["chunks_total"] == 2
```

- [ ] **Step 2: Run test verify pass**

```bash
docker compose exec backend pytest tests/test_chunk_retry.py::test_chunks_done_counter_excludes_retry_sub_chunks -v
```

Expected: PASS（Task 5 implementation 應該已正確處理；如 fail 需修 _transcribe_all_chunks）

- [ ] **Step 3: Commit**

```bash
git -C "D:/vibevoice_asr" add backend/tests/test_chunk_retry.py
git -C "D:/vibevoice_asr" commit -m "test(chunks): 驗證 chunks_done 不含 retry sub-chunks"
git -C "D:/vibevoice_asr" push origin main
```

---

## Task 7: 收尾 — lint / typecheck / 實機驗收

**Files:** N/A（純驗證）

- [ ] **Step 1: lint / typecheck 全綠**

```bash
docker compose exec backend ruff check app/
docker compose exec backend mypy app/
docker compose exec backend pytest tests/ -v 2>&1 | tail -10
docker compose exec backend bandit -r app/ -ll
```

Expected:
- ruff: All checks passed!
- mypy: 0 errors
- pytest: 既有 219 + 6 新加 = 225 passed
- bandit: 0 high+/medium

- [ ] **Step 2: 實機驗收（user 在 Linux 跑）**

對之前測過的 10 分鐘音檔（`華厚%`）重新上傳、驗收標準：

```bash
sqlite3 -header -column /opt/vibevoice_asr/data/app.db <<'EOF'
WITH job AS (
  SELECT id, segments FROM jobs WHERE filename LIKE '華厚%' ORDER BY created_at DESC LIMIT 1
)
SELECT chunks_total, chunks_done, json_array_length(segments) AS n_segs, error
FROM jobs WHERE id = (SELECT id FROM job);
EOF
```

Expected:
- `chunks_total = 13`（不變）
- `n_segs >= 100`（之前 minimal 是 97、retry 後應更多）
- `error` 為 NULL 或最多 1 chunk partial（< 8% 比例）
- worker log 顯示「Repetition detected」次數大幅下降（retry 後成功 → fail 比例降）

時間軸 gap 檢查：

```bash
sqlite3 -header -column /opt/vibevoice_asr/data/app.db <<'EOF'
WITH job AS (
  SELECT id, segments FROM jobs WHERE filename LIKE '華厚%' ORDER BY created_at DESC LIMIT 1
)
SELECT
  printf('%6.1f', json_extract(s.value, '$.start_time')) AS t_start,
  printf('%6.1f', json_extract(s.value, '$.end_time'))   AS t_end,
  json_extract(s.value, '$.speaker_id')                  AS sp,
  substr(json_extract(s.value, '$.text'), 1, 60)         AS text
FROM job, json_each(job.segments) s
ORDER BY t_start;
EOF
```

Expected: 相鄰 segment 間 gap 全部 < 5s（之前有多個 30-50s gap）

- [ ] **Step 3: Commit 收尾文件更新**

更新 `docs/superpowers/backlog/2026-05-10-asr-quality-risks.md` § 2.2 加 M5 完整版完成的紀錄、ship M6 brainstorming 的前置條件已達成。

```bash
git -C "D:/vibevoice_asr" add docs/superpowers/backlog/2026-05-10-asr-quality-risks.md
git -C "D:/vibevoice_asr" commit -m "docs: M5 完整版 ship、M6 brainstorming 前置條件達成"
git -C "D:/vibevoice_asr" push origin main
```

---

## 執行注意事項

1. **每個 commit 的 git 命令拆 3 條**（add / commit / push），避開 Claude Code sandbox 對 chain 的攔截。
2. **每個 task 完成後 user 端要 git pull + rebuild backend image** 才能跑下一個 task 的 test：

```bash
cd /opt/vibevoice_asr
git pull
docker compose up -d --build --force-recreate backend worker
```

3. **跨 task 的 test fixture**（譬如 `chunk_factory`）放在 `test_chunk_retry.py` 頂部、conftest.py 不必改。

4. 若任一 step 的 test 在 Step 4 (verify pass) 失敗 → 不要 commit、修到通過再 commit。違反 TDD 紀律。

5. **lint regression 修補**：每 task 結束後 ruff / mypy 都要 0 errors 才能繼續下個 task。違反 lint-and-validate skill 規範。
