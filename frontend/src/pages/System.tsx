/**
 * System status dashboard.
 *
 * See SPEC.md §8.3.9.
 * M2 milestone (basic) → M7 (full).
 */
export default function System() {
  // TODO(M2): GPU info, vLLM status, Profile, Queue depth
  return (
    <div className="space-y-4 max-w-3xl">
      <h1 className="text-2xl font-bold">系統狀態</h1>
      <div className="text-sm text-gray-500">TODO: M2/M7 milestone</div>
    </div>
  );
}
